# Introduction

Physically based rendering의 핵심 목표는 장면 내부에서 발생하는 빛의 전달을 정확하게 계산하는 것이다. 이를 기술하는 Rendering Equation은 표면에서 방출되거나 반사되는 radiance가 다른 위치와 방향에서 도달하는 radiance에 의해 결정된다는 관계를 정의한다 [[Kajiya 1986]](https://doi.org/10.1145/15886.15902). 그러나 실제 장면에서 이 적분은 매우 높은 차원을 가지며, visibility, complex material, multiple scattering과 같은 요인으로 인해 해석적으로 계산하기 어렵다. 이러한 이유로 현대의 physically based renderer는 대부분 Monte Carlo integration에 기반한 Path Tracing을 사용한다.

Path Tracing은 매우 일반적이며 복잡한 light transport를 계산할 수 있다는 장점이 있다 [[Kajiya 1986]](https://doi.org/10.1145/15886.15902). 하지만 Monte Carlo estimator의 오차는 sample 수가 증가함에 따라 느리게 감소한다. 따라서 낮은 sample 수에서는 높은 variance가 발생하고, 이를 줄이기 위해서는 많은 ray와 light path를 추적해야 한다. 특히 실시간 렌더링에서는 한 pixel에 사용할 수 있는 ray의 수가 제한되기 때문에 충분한 sample을 계산하기 어렵고, 최근의 real-time path tracing은 적은 sample과 spatiotemporal reuse 또는 denoising을 결합하여 이를 보완한다 [[Ouyang et al. 2021]](https://doi.org/10.1111/cgf.14378). 결국 rendering quality, variance, computation cost 사이의 trade-off는 여전히 physically based real-time rendering의 중요한 제약으로 남아 있다.

지난 수십 년 동안 이러한 계산 비용을 줄이기 위한 다양한 방법이 개발되었다. Precomputed Radiance Transfer는 light transport를 미리 계산하여 저차원 basis에 저장하고 동적인 low-frequency lighting에서 재사용하였다 [[Sloan et al. 2002]](https://doi.org/10.1145/566570.566612). Radiance Caching은 계산된 directional incoming radiance를 공간적으로 저장하고 주변 query에서 보간하여 다시 사용한다 [[Křivánek et al. 2005]](https://doi.org/10.1109/TVCG.2005.83). Path Guiding은 이전 light path에서 얻은 정보를 이용하여 scene의 spatio-directional radiance field를 근사하고 이후 sampling distribution을 개선한다 [[Müller et al. 2017]](https://doi.org/10.1111/cgf.13227). ReSTIR는 인접 pixel과 이전 frame에서 얻은 candidate sample을 재사용하고 [[Bitterli et al. 2020]](https://doi.org/10.1145/3386569.3392481), ReSTIR GI는 이러한 원리를 multi-bounce indirect path로 확장하여 중요한 path 정보를 pixel과 frame 사이에서 공유한다 [[Ouyang et al. 2021]](https://doi.org/10.1111/cgf.14378). Neural Radiance Caching은 Path Tracing 과정에서 생성되는 radiance sample을 online neural representation에 학습하여, 이후의 light path가 expensive한 indirect transport를 계속 추적하는 대신 학습된 radiance를 query할 수 있도록 한다 [[Müller et al. 2021]](https://doi.org/10.1145/3450626.3459812).

이들 방법은 서로 다른 알고리즘을 사용하지만 하나의 공통적인 특성을 가진다. 이전에 계산된 light transport information이 이후의 rendering query에서도 유용할 수 있다는 것이다. Radiance Caching은 spatial coherence를, Path Guiding은 spatial-directional structure를, ReSTIR 계열은 spatial-temporal sample reuse를, Neural Radiance Caching은 radiance function의 learned reuse를 이용한다 [[Křivánek et al. 2005]](https://doi.org/10.1109/TVCG.2005.83) [[Müller et al. 2017]](https://doi.org/10.1111/cgf.13227) [[Bitterli et al. 2020]](https://doi.org/10.1145/3386569.3392481) [[Müller et al. 2021]](https://doi.org/10.1145/3450626.3459812). 이러한 연구들의 성공은 서로 다른 light transport query가 항상 완전히 독립적인 문제가 아니며, 이전 계산에서 얻은 일부 정보를 새로운 계산에 재사용할 수 있음을 보여준다.

그러나 이러한 사실을 light transport 전체가 하나의 작은 저차원 표현으로 압축될 수 있다는 주장으로 확대해서는 안 된다. 서로 다른 rendering query 사이에 exploitable correlation이 존재한다는 것과 전체 Light Transport Operator가 항상 low-rank 또는 finite-dimensional representation으로 근사될 수 있다는 것은 서로 다른 문제이다. 실제로 Light Transport Operator의 compactness를 이론적으로 분석한 연구에서는 일반적인 operator가 non-compact하며, arbitrary lighting에 대해 고정된 finite-rank 또는 non-adaptive finite basis approximation의 error를 uniform하게 제한할 수 없음을 보였다. 이 결과는 purely diffuse transport에 해당하는 낮은 차원의 경우에도 특별한 조건을 제외하면 유지된다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725). 이후의 spectral analysis 역시 이러한 non-compact structure를 바탕으로 Light Transport Operator의 spectral properties를 분석한다 [[Soler et al. 2025]](https://doi.org/10.1145/3774756).

따라서 light transport는 완전히 독립적인 문제들의 집합도 아니지만, 동시에 하나의 작은 representation으로 항상 압축 가능한 단순한 함수도 아니다. 특정 장면과 transport regime에서는 강한 spatial, directional, temporal coherence가 존재하는 반면, visibility discontinuity, high-frequency illumination 및 복잡한 transport는 고정된 저차원 근사를 어렵게 만들 수 있다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725).

이러한 관점에서 Monte Carlo rendering의 계산 과정을 다시 바라볼 필요가 있다. 서로 다른 light path는 동일한 경로를 단순히 반복해서 계산하지 않는다. 각각의 sample은 서로 다른 위치와 방향을 통과하며 서로 다른 contribution을 생성한다. 따라서 이를 단순한 duplicated computation이라고 표현하는 것은 정확하지 않다. 그러나 Radiance Caching, Path Guiding, ReSTIR와 Neural Radiance Caching의 결과는 서로 다른 sample과 query에서 얻은 정보가 이후의 estimation에 유용할 수 있음을 반복적으로 보여준다 [[Křivánek et al. 2005]](https://doi.org/10.1109/TVCG.2005.83) [[Müller et al. 2017]](https://doi.org/10.1111/cgf.13227) [[Ouyang et al. 2021]](https://doi.org/10.1111/cgf.14378) [[Müller et al. 2021]](https://doi.org/10.1145/3450626.3459812). 즉 문제를 동일한 연산의 단순한 반복이라기보다, 서로 관련된 light transport quantities를 반복적으로 estimation하는 과정으로 보는 것이 보다 정확하다.

이러한 특성은 기존 Monte Carlo 기반 rendering pipeline에 대해 근본적인 질문을 제기한다. 현재의 renderer는 새로운 pixel이나 frame을 계산할 때마다 새로운 light path를 생성하고 transport를 estimation한다. 기존 연구들은 precomputation, caching, distribution learning, sample reuse와 같은 방법을 통해 이러한 computation의 일부를 성공적으로 재사용해왔다 [[Sloan et al. 2002]](https://doi.org/10.1145/566570.566612) [[Müller et al. 2017]](https://doi.org/10.1111/cgf.13227) [[Bitterli et al. 2020]](https://doi.org/10.1145/3386569.3392481). 하지만 Path Guiding, ReSTIR 및 Neural Radiance Caching과 같은 대표적인 방법들은 여전히 Monte Carlo Path Tracing을 기본적인 light transport computation framework로 유지하면서 그 sampling 또는 estimation 과정의 일부를 개선하거나 재사용한다.

최근 Neural Rendering 연구는 이 구조와 다른 가능성도 탐색하기 시작했다. RenderFormer는 triangle mesh, material 및 lighting information으로부터 global illumination을 직접 예측하는 transformer-based formulation을 제안하여 explicit ray-traced light transport와 다른 접근을 탐색하였다 [[Zeng et al. 2025]](https://doi.org/10.1145/3721238.3730595). 최근의 Generalizable Light Transport 3D Embedding은 3D scene configuration으로부터 global light transport representation을 생성하고 local query를 통해 global illumination을 예측하는 방법을 제안하였다 [[Xu et al. 2026]](https://doi.org/10.1145/3799902.3811095). 이러한 연구들은 light transport computation이 반드시 모든 query에서 독립적인 Monte Carlo sampling만으로 수행되어야 하는 것은 아닐 가능성을 보여준다. 동시에 large-scale training cost, representation capacity, generalization, high-frequency transport 및 dynamic scene support와 같은 문제는 여전히 중요한 제약으로 남는다. 따라서 neural network가 기존 light transport algorithms을 일반적으로 대체할 수 있다는 결론은 현재의 증거만으로는 성립하지 않는다.

보다 중요한 질문은 특정 neural architecture를 선택하는 것이 아니라, light transport computation 자체가 어떠한 구조를 가지고 있는지를 이해하는 것이다. 어떤 정보가 새로운 rendering query에서도 재사용될 수 있는지, 어떤 정보는 새롭게 계산되어야 하는지, 그리고 이러한 구조를 explicit cache, analytical representation, Monte Carlo sampling 또는 learned representation 가운데 어떤 방식으로 표현하는 것이 효율적인지는 개별 transport regime과 계산 환경에 따라 달라질 수 있다.

본 연구의 장기적인 연구 비전은 이러한 질문에서 출발한다.

> **Next Generation of Light Transport for Realistic and High-Performance Rendering**

이 비전의 목적은 단순히 기존 Path Tracing을 neural network로 대체하는 것이 아니다. 또한 neural network를 사용한다는 사실 자체를 새로운 rendering paradigm으로 간주하지 않는다. 대신 현대 GPU가 제공하는 ray-tracing hardware, massively parallel computation, tensor processing capability와 새로운 learned representation의 가능성을 고려하여, 기존 Monte Carlo 중심의 light transport computation 이후에 어떠한 새로운 계산 방식이 가능할지를 탐구하는 것이 목표이다.

이를 위해 가장 먼저 필요한 것은 해결책을 결정하는 것이 아니라 현재 light transport computation의 본질적인 구조와 한계를 이해하는 것이다. Path Tracing, Precomputed Radiance Transfer, Radiance Caching, Path Guiding, ReSTIR, Neural Radiance Caching과 최근의 learned light transport methods를 하나의 연속적인 관점에서 분석하고, 이들이 각각 어떤 정보를 새롭게 계산하며 어떤 정보를 재사용하는지를 이해해야 한다. 이러한 분석을 통해서만 향후의 light transport algorithm이 기존 Monte Carlo estimator의 단순한 확장인지, 혹은 실제로 다른 computation paradigm으로 발전할 수 있는지를 객관적으로 판단할 수 있다.

이러한 문제를 보다 근본적으로 이해하기 위해서는 특정 rendering algorithm을 비교하는 것에서 한 단계 더 나아가, 장면 자체가 빛의 전달을 어떻게 규정하는지를 살펴볼 필요가 있다. Rendering Equation은 연산자 관점에서 다음과 같이 표현할 수 있다.

$$
L = L_e + TL
$$

여기서 $L_e$는 장면에 주어진 emitted radiance이고, $T$는 한 번의 scattering과 propagation을 통해 radiance가 장면 안에서 어떻게 전달되는지를 나타내는 Light Transport Operator이다. 이 관점에서 중요한 점은 실제 radiance $L$와 이를 만들어내는 transport rule $T$를 구분하는 것이다. 특정 광원은 $L_e$를 통해 장면에 에너지를 공급하고, camera는 최종적으로 형성된 radiance의 일부를 관측한다. 반면 $T$ 자체는 장면의 geometry, visibility, material 및 participating media와 같은 물리적 구성에 의해 결정된다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725) [[Soler et al. 2025]](https://doi.org/10.1145/3774756).

따라서 "장면 내부에 camera나 light source와 독립적인 light transport structure가 존재하는가?"라는 질문에 대해서는 이미 비교적 명확한 답을 내릴 수 있다. 장면은 Light Transport Operator $T$라는 형태로 고유한 transport rule을 가진다. 이는 새로운 관찰이 아니다. 고전적인 Radiosity의 form factor matrix 역시 diffuse transport라는 제한된 조건 아래에서 surface 사이의 geometry-dependent transfer를 명시적으로 표현하였으며, Precomputed Radiance Transfer는 이러한 scene-dependent transfer를 illumination과 분리하여 미리 계산하고 재사용하였다 [[Sloan et al. 2002]](https://doi.org/10.1145/566570.566612). 최근의 operator-theoretic analysis는 이를 보다 일반적인 continuous light transport의 관점에서 다루고 있다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725) [[Soler et al. 2025]](https://doi.org/10.1145/3774756).

그러나 scene-dependent transport operator가 존재한다는 사실과, 그 operator를 실제 rendering에서 효율적으로 사용할 수 있는 표현이 존재한다는 것은 서로 다른 문제이다. 일반적인 Light Transport Operator는 position과 direction에 걸쳐 정의되는 고차원 연산자이며, visibility discontinuity와 directional material response로 인해 매우 복잡한 구조를 가질 수 있다. 특히 일반적인 operator가 non-compact하다는 이론적 결과는 전체 light transport를 작은 수의 basis나 고정된 low-rank representation으로 항상 표현할 수 있다는 가정을 부정한다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725). 따라서 scene-intrinsic transport structure의 존재 자체보다 중요한 문제는, 이 복잡한 operator 안에서 실제 rendering computation에 활용할 수 있는 제한적이고 반복 가능한 구조가 무엇인지 파악하는 것이다.

이 문제를 이해하기 위한 첫 번째 exploratory experiment에서는 특정 neural representation이나 spectral model을 가정하지 않고, Monte Carlo Path Tracing에서 실제로 생성되는 path ensemble 자체를 관찰하였다. 초기 질문은 단순하였다. 하나의 장면에서 매우 많은 light path를 추적했을 때, 서로 다른 path 사이에서 반복적으로 나타나는 공간적 구조가 존재하는가? 이를 확인하기 위해 Cornell Box에서 Russian Roulette를 제거하고 충분히 큰 maximum depth를 설정한 뒤, 서로 독립적인 camera-origin BSDF random walk를 반복적으로 생성하였다. 각 path에서 방문한 surface position과 bounce depth를 기록하고, 많은 path가 장면 내부의 어느 영역을 얼마나 자주 방문하는지를 spatial distribution으로 분석하였다.

실험 결과는 처음 예상했던 "특정한 하나의 light path가 반복적으로 나타나는 형태"와는 달랐다. 서로 다른 Monte Carlo sample은 서로 다른 trajectory를 생성하였으며, 동일한 line path가 반복적으로 등장하지 않았다. 대신 많은 path를 하나의 ensemble로 보았을 때 장면 geometry와 관련된 안정적인 spatial visitation distribution이 형성되는 현상이 관찰되었다. 즉 반복적으로 나타난 것은 특정 path 자체가 아니라, 다수의 random walk가 만들어내는 통계적 분포였다. 이 관찰은 연구 질문을 "특정한 transport path가 존재하는가?"에서 "서로 다른 path들의 ensemble에 지속적으로 나타나는 statistical structure가 존재하는가?"로 변화시켰다.

그러나 하나의 camera에서 생성된 path만으로는 이러한 구조가 장면 자체에서 발생한 것인지, 특정 camera configuration에서 발생한 것인지 구분할 수 없다. Camera에서 시작하는 Path Tracing random walk의 초기 distribution은 sensor position과 viewing direction에 강하게 의존하기 때문이다. 따라서 다음 실험에서는 동일한 Cornell Box에 서로 다른 세 camera를 배치하고, 각각에서 독립적인 random walk ensemble을 생성하였다. 동시에 같은 camera를 서로 다른 random seed로 반복하여 Monte Carlo sampling noise의 크기를 측정하였다.

초기 bounce에서는 예상한 것처럼 camera마다 매우 다른 spatial distribution이 나타났다. 서로 다른 camera 사이의 full 3D spatial similarity는 동일한 camera를 서로 다른 random seed로 반복했을 때의 similarity보다 크게 낮았다. 따라서 path ensemble은 처음에는 자신이 어떤 camera에서 시작했는지에 대한 강한 정보를 가지고 있었다. 그러나 bounce depth가 증가하면서 이 차이는 빠르게 감소하였다. 특히 intermediate bounce에서는 서로 다른 camera 사이의 distribution 차이가 동일한 camera에서 random seed만 변경했을 때 발생하는 Monte Carlo noise와 비슷한 수준까지 감소하였다.

이 현상은 직관적으로 다음과 같이 이해할 수 있다. 서로 다른 camera에서 시작한 path들은 초기에는 서로 다른 scene region을 방문하지만, 반복적인 scattering과 visibility interaction을 거치면서 initial camera configuration에 대한 영향을 점차 잃는다. 이후 surviving path들의 spatial visitation statistics는 camera보다는 scene의 geometry와 material interaction에 더 크게 영향을 받는 형태로 변화한다. 본 연구에서는 이러한 현상을 편의상 camera forgetting 또는 empirical mixing이라고 표현하였다. 그러나 여기서 중요한 점은 이것이 physical Light Transport Operator의 mixing을 의미한다고 주장하지 않는다는 것이다. 실험에서 생성된 trajectory는 BSDF sampling probability에 의해 선택된 Monte Carlo random walk이며, 실제 radiance transport operator $T$와 동일한 object가 아니다.

이 구분은 매우 중요하다. Monte Carlo Path Tracing에서 physical transport와 sampling process는 밀접하게 연결되어 있지만 동일하지 않다. Rendering Equation의 transport operator는 radiance가 geometry와 BSDF를 통해 어떻게 전달되는지를 나타내는 반면, Monte Carlo sampler는 그 integral을 추정하기 위해 어떤 path를 얼마나 자주 선택할지를 결정한다. 따라서 random-walk visitation distribution에서 camera dependence가 감소한다는 관찰만으로 Light Transport Operator의 eigenmode, invariant distribution 또는 spectral convergence를 직접 주장할 수 없다. 현재의 실험 결과는 단지 scene-dependent statistical regularity가 sampling trajectories에서도 관찰된다는 empirical evidence로 해석되어야 한다.

이러한 camera forgetting 현상을 보다 단순한 dynamical model로 설명할 수 있는지를 확인하기 위해, 다음 실험에서는 Cornell Box의 surface들을 소수의 discrete state로 축약하였다. Floor, Ceiling, BackWall, LeftWall, RightWall 및 box surface 등을 각각 하나의 state로 정의하고, random walk가 어떤 surface에서 어떤 surface로 이동하는지를 transition matrix로 표현하였다. 또한 path가 scene 밖으로 빠져나가거나 emitter에 도달하면 trajectory가 종료되므로, ordinary stationary distribution보다는 surviving trajectories에 조건부인 quasi-stationary distribution을 사용하여 late-bounce surface distribution을 분석하였다.

흥미롭게도 이 매우 단순한 surface-level model은 실제 late-bounce distribution의 형태를 어느 정도 설명할 수 있었다. 서로 다른 camera에서 생성된 independent validation trajectories는 intermediate bounce에서 training trajectories로부터 계산된 quasi-stationary distribution과 가까워졌다. 이는 앞서 관찰한 camera forgetting 현상이 단순한 시각적 artifact만은 아니며, repeated random walk에 의해 생성되는 비교적 안정적인 coarse distribution이 존재할 가능성을 보여주었다.

그러나 동일한 모델은 transport dynamics 자체를 정확하게 설명하지 못했다. Quasi-stationary model의 dominant eigenvalue가 예측한 path survival rate와 실제 Monte Carlo trajectories에서 측정된 late-bounce survival rate 사이에는 명확한 차이가 있었으며, 두 번째 eigenvalue로부터 예상되는 spectral convergence rate 역시 실제 distribution의 convergence와 일치하지 않았다. 즉 coarse surface distribution의 모양은 어느 정도 비슷했지만, 해당 distribution이 bounce depth에 따라 어떻게 변화하는지를 surface ID만으로는 정확하게 예측할 수 없었다.

이 결과는 매우 중요한 한계를 보여준다. 하나의 surface는 실제로 매우 많은 transport state를 포함한다. 예를 들어 동일한 BackWall에 도달한 두 path라도 surface의 어느 위치에 도달했는지, 어떤 방향에서 입사했는지, 그리고 현재 방향이 무엇인지에 따라 다음 bounce에서 방문하게 될 geometry가 크게 달라질 수 있다. 이를 모두 단순히 "BackWall"이라는 하나의 state로 합치면 future transport를 예측하는 데 필요한 정보가 사라질 수 있다.

일반적인 light transport가 position-direction phase space 위에서 정의된다는 사실은 이러한 관찰과 자연스럽게 연결된다. Surface transport에서 radiance는 일반적으로

$$
L(x,\omega)
$$

와 같이 position $x$와 direction $\omega$의 함수로 표현된다. 따라서 surface ID와 같이 지나치게 coarse한 projection은 original transport dynamics가 가지고 있던 directional 또는 spatial memory를 제거할 수 있다. 그러나 이것이 곧 $(x,\omega)$ 전체가 반드시 사용되어야 한다거나, $(x,\omega)$가 rendering을 위한 최소 representation이라는 것을 의미하지는 않는다. 오히려 이 실험이 제기하는 질문은 "어떤 정보를 제거할 때 transport dynamics가 깨지기 시작하는가?"에 가깝다.

이러한 문제는 Markov process의 state aggregation 또는 lumpability와도 개념적으로 연결된다. 고차원 state들을 소수의 coarse state로 합쳤을 때, 동일한 coarse state에 속하는 모든 microscopic state가 future transition에 대해 충분히 유사하지 않다면 reduced process는 더 이상 memoryless Markov process로 표현되지 않을 수 있다. Light transport에서도 position과 direction을 과도하게 제거할 경우 비슷한 문제가 발생할 가능성이 있다. 또한 statistical mechanics의 coarse-graining이나 Mori-Zwanzig formalism과 같이 unresolved variable을 제거했을 때 reduced dynamics에 memory effect가 나타나는 이론 역시 이러한 현상을 분석하기 위한 잠재적인 수학적 도구가 될 수 있다. 그러나 현재 실험만으로 surface-level model의 실패가 이러한 특정 이론에 의해 완전히 설명된다고 주장할 수는 없다.

Cornell Box 하나에서 관찰된 현상이 단순한 closed diffuse environment의 특수한 결과인지 확인하기 위해, 동일한 experimental protocol을 여러 장면으로 확장하였다. Cornell Box뿐 아니라 Kitchen, Glass-of-Water, Staircase2 및 Car와 같이 geometry와 material 특성이 서로 다른 장면을 사용하고, 각 장면마다 원래 camera와 서로 다른 두 alternative camera에서 path ensemble을 생성하였다. Scene scale이 서로 다르기 때문에 각 장면의 bounding box를 이용하여 spatial position을 정규화하였으며, 모든 distribution comparison은 동일한 scene 내부의 camera들 사이에서만 수행하였다.

Cross-scene experiment에서도 하나의 공통적인 경향이 나타났다. 테스트된 모든 장면에서 early bounce의 camera dependence가 intermediate bounce에서 감소하였다. 그러나 그 감소 속도와 noise floor에 도달하는 정도는 장면마다 달랐다. Cornell Box와 Kitchen에서는 cross-camera difference가 independent-seed variation과 비슷한 수준까지 감소하였고, Glass-of-Water에서도 다소 늦은 bounce에서 비슷한 경향이 나타났다. Staircase2에서도 camera dependence가 크게 감소했지만 보수적인 기준에서는 일부 residual difference가 남았으며, Car에서는 초기 camera dependence가 크게 감소했음에도 다른 장면보다 상대적으로 더 많은 residual camera information이 유지되었다.

이 결과는 두 가지 점에서 중요하다. 첫째, camera forgetting 현상이 Cornell Box 하나의 특수한 현상만으로 설명되기는 어렵다. 복잡한 realistic geometry와 specular 또는 refractive transport가 포함된 장면에서도 비슷한 방향의 변화가 관찰되었다. 둘째, 모든 장면이 동일한 방식으로 동작하지 않았다. 따라서 multiple scattering이 존재한다는 사실만으로 하나의 universal mixing rate를 가정할 수 없으며, scene geometry, visibility, material response, escape probability 및 surviving trajectory의 구성 등이 함께 작용할 가능성이 있다.

특히 material의 diffuse/specular 구분만으로 observed behavior를 설명하기 어렵다는 점도 확인되었다. Glass-of-Water에서는 sampled continuation 중 delta event의 비율이 매우 높았음에도 camera dependence가 bounce와 함께 크게 감소하였다. 반대로 delta event의 비율이 상대적으로 낮은 Car에서는 더 큰 residual camera dependence가 관찰되었다. 따라서 단순히 diffuse scattering은 빠르게 정보를 잃고 specular scattering은 정보를 오래 유지한다는 설명만으로는 충분하지 않다. Light transport의 long-range statistical behavior는 geometry, directional constraints, visibility 및 termination structure를 함께 고려해야 할 가능성이 있다.

동시에 path survival statistics 역시 장면에 따라 크게 달랐다. Kitchen에서는 매우 긴 trajectories가 빈번하게 나타난 반면, Glass-of-Water와 Car에서는 대부분의 path가 짧은 bounce 내에 종료되지만 드물게 매우 긴 trajectory가 발생하였다. 따라서 late-bounce distribution은 전체 path population의 특성을 그대로 나타내는 것이 아니라, 해당 bounce까지 살아남은 trajectory에 조건부인 survivor distribution이라는 점을 고려해야 한다. 이러한 survivor bias는 특히 open scene이나 strong specular interaction이 포함된 장면에서 observed mixing을 해석할 때 중요한 제한이 된다.

지금까지의 exploratory experiments를 종합하면 몇 가지 사실과 아직 해결되지 않은 질문을 구분할 수 있다. 먼저 서로 다른 Monte Carlo path가 완전히 독립적인 statistical object는 아니며, 여러 장면에서 path ensemble은 scene-dependent spatial structure를 나타낸다. 또한 camera-origin random walk의 초기 camera dependence는 repeated scattering 이후 감소하는 경향을 보인다. 이 현상은 Cornell Box에만 국한되지 않으며, 서로 다른 geometry와 material을 가진 여러 장면에서 관찰되었다. 반면 이러한 behavior의 속도와 residual dependence는 장면마다 다르며, surface ID만을 상태로 사용한 coarse dynamical model은 실제 multi-bounce dynamics를 정확하게 설명하지 못하였다.

그러나 이러한 결과를 Light Transport Operator 자체의 구조와 직접 동일시해서는 안 된다. 현재 실험은 physical radiance transfer가 아니라 Monte Carlo BSDF sampling process의 trajectory statistics를 분석한다. 따라서 observed camera forgetting이 $T^n$의 spectral convergence에 의해 발생하는지, sampling distribution의 특성에 의해 발생하는지, 혹은 두 구조가 어떤 방식으로 연결되어 있는지는 아직 밝혀지지 않았다. 이 구분은 이후 연구에서 가장 먼저 해결해야 하는 중요한 문제이다.

이러한 실험 결과와 기존 이론을 함께 고려하면, 연구의 초점 역시 보다 명확하게 수정될 수 있다. "Scene에 고유한 light transport structure가 존재하는가?"라는 질문 자체는 이미 Light Transport Operator라는 개념을 통해 답해져 있다. 또한 전체 light transport가 일반적으로 작은 low-dimensional representation으로 압축될 수 있는가라는 질문 역시 non-compactness에 대한 기존 이론 때문에 긍정적으로 가정할 수 없다 [[Soler et al. 2022]](https://doi.org/10.1145/3528233.3530725). 따라서 보다 중요한 질문은 이 두 극단 사이에 존재한다.

즉 일반적으로 매우 복잡한 Light Transport Operator 안에서,

> **실제 rendering computation에서 반복적으로 활용할 수 있는 제한적이고 scene-intrinsic한 structure는 무엇인가?**

라는 질문이다.

이러한 structure는 하나의 형태로 미리 가정되어서는 안 된다. 특정 transport regime에서는 spatial coherence일 수 있고, 다른 경우에는 directional coherence, sparse connectivity, local low-rank structure, hierarchical interaction, recurrent path family 또는 spectral structure일 수 있다. 또한 이러한 structure가 반드시 global하게 존재할 필요도 없다. 특정 spatial region, directional range, bounce range 또는 frequency range에서만 나타나는 local structure도 rendering computation에 충분히 유용할 수 있다.

과거의 여러 rendering method는 이미 이러한 제한적 구조를 각기 다른 방식으로 이용해왔다. Radiosity는 diffuse surface-to-surface transfer를 finite-dimensional matrix로 근사하였다. PRT는 low-frequency illumination이라는 restriction 아래 transport를 reusable basis representation으로 저장하였다 [[Sloan et al. 2002]](https://doi.org/10.1145/566570.566612). Radiance Caching은 local spatial smoothness를 이용하였고 [[Křivánek et al. 2005]](https://doi.org/10.1109/TVCG.2005.83), Path Guiding은 local spatio-directional radiance distribution을 학습하였다 [[Müller et al. 2017]](https://doi.org/10.1111/cgf.13227). ReSTIR는 neighboring query 사이에서 useful sample information을 공유하며 [[Bitterli et al. 2020]](https://doi.org/10.1145/3386569.3392481), Neural Radiance Caching은 특정 illumination 아래에서 형성된 radiance function을 learned representation으로 재사용한다 [[Müller et al. 2021]](https://doi.org/10.1145/3450626.3459812).

이들을 하나의 관점에서 바라보면, 성공적인 rendering acceleration은 전체 Light Transport Operator를 완전히 표현하는 대신 특정 계산 조건에서 필요한 transport structure의 일부만을 선택적으로 표현하거나 재사용해왔다고 볼 수 있다. 따라서 향후 연구의 핵심은 모든 light transport를 하나의 universal representation에 넣는 것이 아니라, 어떤 transport information이 어떤 조건에서 persistent하고 reusable하며, 그 정보를 유지하기 위해 필요한 state와 representation이 무엇인지를 찾는 데 있을 수 있다.

이 관점에서 본 연구의 초기 실험에서 관찰된 camera forgetting은 최종 연구 주제라기보다 하나의 empirical clue로 해석하는 것이 적절하다. 서로 다른 camera에서 시작한 random walk가 repeated scattering 이후 비슷한 spatial visitation statistics를 보인다는 것은 transport computation 안에 query-specific information과 scene-dependent information이 서로 다른 bounce scale에서 존재할 가능성을 보여준다. 그러나 이것이 실제 physical transport에서도 동일하게 성립하는지, 어떤 representation에서 가장 명확하게 나타나는지, 그리고 rendering acceleration에 활용할 수 있을 정도로 강한 구조인지는 추가적인 검증이 필요하다.

따라서 다음 단계에서 바로 새로운 neural architecture나 새로운 Monte Carlo estimator를 설계하는 것은 시기상조이다. 먼저 sampling random walk에서 관찰된 statistical structure와 physical Light Transport Operator 사이의 관계를 분리하여 이해할 필요가 있다. 작은 controlled scene에서 실제 transport operator 또는 그 finite discretization을 직접 구성하고, illumination 및 camera query와 독립적으로 어떤 spatial, directional, spectral 또는 local structure가 존재하는지를 관찰하는 것이 하나의 가능한 다음 단계이다. 이 과정에서 $T$, $T^2$, $T^3$와 같이 repeated transport가 진행될 때 어떤 information이 유지되고 어떤 information이 빠르게 소실되는지도 분석할 수 있다.

이러한 분석의 목적은 특정 spectral model을 증명하거나 Markov mixing을 연구 주제로 고정하는 것이 아니다. 오히려 다양한 representation 후보가 실제 light transport의 어떤 특성을 보존하고 어떤 특성을 잃는지를 비교함으로써, general light transport 안에서 exploitable structure가 어디에 존재하는지를 찾아가는 것이다. 만약 그러한 structure가 low-frequency basis로 충분하다면 PRT와 유사한 analytical representation이 적절할 수 있다. Local sparse structure가 지배적이라면 hierarchical 또는 sparse transport representation이 적절할 수 있다. 매우 높은 차원의 nonlinear structure가 필요하지만 compact하게 근사할 수 있다면 learned representation이 비로소 정당화될 수 있다. 반대로 충분히 reusable한 structure가 존재하지 않는 transport regime에서는 Monte Carlo sampling 자체가 여전히 가장 합리적인 computation paradigm일 수도 있다.

따라서 본 연구의 현재 단계에서는 neural network, spectral decomposition, caching 또는 Monte Carlo 중 어느 하나를 최종 해결책으로 선택하지 않는다. 이들은 모두 light transport structure를 표현하거나 계산하기 위한 서로 다른 도구이다. 먼저 해결해야 할 문제는 그 이전에 존재한다.

> **일반적으로 복잡한 Light Transport 안에서 실제 rendering 계산에 활용할 수 있는 scene-intrinsic structure는 무엇이며, 그 structure는 어떠한 조건에서 유지되고 어떠한 조건에서 붕괴하는가?**

이 질문은 처음 제시한 Next Generation of Light Transport라는 장기적인 연구 비전을 보다 구체적인 연구 방향으로 연결한다. 기존 rendering 연구의 역사는 이미 전체 계산을 반복하는 대신 특정한 transport information을 재사용함으로써 큰 성능 향상을 얻을 수 있음을 반복적으로 보여주었다. 동시에 Light Transport Operator의 이론적 복잡성은 하나의 universal low-dimensional representation이 모든 transport를 해결할 것이라는 단순한 가정을 허용하지 않는다. 따라서 차세대 light transport algorithm의 핵심은 모든 transport를 하나의 방법으로 대체하는 것이 아니라, scene과 transport regime에 존재하는 reusable structure를 정확하게 식별하고, 그 구조에 가장 적합한 computation mechanism을 선택하는 데 있을 가능성이 있다.

현재까지의 결과는 아직 새로운 rendering algorithm을 제시하지 않는다. 오히려 어떤 문제를 풀어야 하는지를 보다 명확하게 만든다. Light transport의 구조가 존재한다는 사실 자체는 이미 알려져 있다. 그러나 그 구조 가운데 어떤 부분이 실제 rendering query 사이에서 지속적으로 재사용 가능하며, 어느 정도의 state information이 그 structure를 보존하기 위해 필요한지, 그리고 그러한 structure를 계산 비용 감소로 연결할 수 있는지는 여전히 열린 문제이다. 본 연구는 이 지점에서 출발하여, 기존 Monte Carlo 중심의 rendering computation 이후에 가능한 새로운 light transport computation paradigm을 탐색하고자 한다.